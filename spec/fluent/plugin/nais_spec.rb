require "spec_helper"

RSpec.describe Fluent::Plugin::Nais do
  it "has a version number" do
    expect(Fluent::Plugin::Nais::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
